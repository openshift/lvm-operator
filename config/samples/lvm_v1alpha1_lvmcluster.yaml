apiVersion: lvm.topolvm.io/v1alpha1
kind: LVMCluster
metadata:
  name: my-lvmcluster
spec:
  storage:
    deviceClasses:
    - name: vg1
      default: true
      deviceAccessPolicy: shared
      fstype: xfs
      deviceSelector:
        paths:
        - /dev/nvme1n1
